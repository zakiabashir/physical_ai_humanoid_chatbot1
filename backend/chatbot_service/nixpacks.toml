[phases.setup]
nixPkgs = ["python311"]

[phases.build]
cmd = "pip install --no-cache-dir -r requirements.txt"

[start]
cmd = "uvicorn src.main:app --host 0.0.0.0 --port $PORT"

[variables]
PORT = "8002"
